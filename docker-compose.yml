services:
  backend:
    build:
      context: ./backend
    container_name: cuentas-backend
    ports:
      - "3000:3000"

  frontend:
    build:
      context: ./frontend
    container_name: cuentas-frontend
    ports:
      - "3001:3000"
    depends_on:
      - backend